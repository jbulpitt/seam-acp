node_modules
dist
.env
.env.local
.env*.bak*
data/
*.log

# Local docs (reports, plans, specs — not committed; tracked files unaffected)
docs/
!docs/agent-guides/
.DS_Store

# Playwright MCP scratch outputs
.playwright-mcp/
*.png

# Untracked dev artifacts
test-file.txt
scripts/acp-bench.ts
scripts/prune-dead-sessions.mjs
scripts/rename-threads.mjs
scripts/fix-cpfs-threads.mjs

# Agent instructions (local only — customize per environment)
# AGENTS.md

# Local OCI provisioning context
oci-context.md

# Local research artifacts
streaming-markdown-research.md
coverage/

# Runtime cache (path→id map) and throwaway debug/probe scripts
cache/
scratch/

# Local cron scripts (host-specific operational glue;
# backed up via the OCI boot-volume snapshot, not git)
crons/
